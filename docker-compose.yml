# Postgres is managed externally.
# DATABASE_URL=postgresql+asyncpg://spec_forge_user:PASSWORD@localhost:5432/spec_forge_db
#
# To run optional services locally (e.g. a fresh Postgres for CI), uncomment below:
#
# services:
#   postgres:
#     image: postgres:16
#     environment:
#       POSTGRES_USER: spec_forge_user
#       POSTGRES_PASSWORD: your_password
#       POSTGRES_DB: spec_forge_db
#     ports:
#       - "5432:5432"
#     volumes:
#       - pgdata:/var/lib/postgresql/data
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U spec_forge_user"]
#       interval: 5s
#       timeout: 5s
#       retries: 5
#
# volumes:
#   pgdata:
